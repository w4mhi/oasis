# OASIS e-ink monitor — Python deps
#
# Cross-platform:
Pillow>=9.0
#
# Pi-only (the real panel). Install on the Raspberry Pi, not on a dev laptop:
#   spidev
#   RPi.GPIO
# plus the vendored Waveshare driver package `waveshare_epd` (epd2in7_V2),
# copied from Waveshare's e-Paper repo into this folder or onto PYTHONPATH.
